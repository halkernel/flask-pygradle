import setuptools

setuptools.setup(name='flaskhelloworld',
                 version='1.2.3',
                 package_dir={'':'src'})

#TODO set up the with `open(...` to the current flask directory)
#TODO set up where the new src is after the previous step